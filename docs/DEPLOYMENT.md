# Deployment provider options and free-tier operating plan

Last reviewed: 2026-09-10

This document records the delivery-provider evaluation for issue
[#53](https://github.com/Patric-1613/AI_daily_digest/issues/53). Pricing and free-tier limits
change independently of this repository, so the delivery owner must recheck the linked provider
pages before creating a resource or approving a cost.

This is an operational recommendation. The team has separately approved exactly one paid resource
— the `ai-daily-digest-intelligence` cron (see "Daily pipeline cron (ingestion + intelligence)") —
and no other. It
is not authorization to create any further account, paid resource, production credential, or real
subscriber campaign. Secrets belong in the deployment provider's secret environment, never in Git,
issue comments, build output, or application logs.

## MVP recommendation

Use the following combination for the seven-day MVP:

| Capability | Provider and plan | MVP use |
|---|---|---|
| React/Vite frontend | Render Static Site, Free | Build and serve the static frontend. |
| FastAPI service | Render Web Service, Free | Serve health/readiness and later `/v1` routes. |
| PostgreSQL | Render Postgres, Free | Temporary MVP/demo data only. |
| Scheduled daily pipeline | Render Cron Job, `0.5c-512mb` (team-approved, min USD 1/month) | Runs `run-daily-digest` daily at 06:00 UTC: collects the three verified RSS sources, then runs intelligence over the real collection window (not a fixed previous-day window), capped at five snapshots as a cost control (issue #53, extended by issue #113). Do not expose the pipeline through a public HTTP trigger. |
| Ad-hoc pipeline runs | Authorised operator's local CLI | Still valid for one-off or debugging runs from an authorised team member's local checkout; never through an unauthenticated public endpoint. |
| Transactional email | Resend, Free | Send confirmation, unsubscribe, and digest test emails over the HTTPS API. |

This selection keeps deployment understandable for a three-person capstone and matches the
existing FastAPI, PostgreSQL, and provider-adapter architecture. It is not a production-readiness
claim.

The repository's `render.yaml` declares the three Free resources needed for the database-backed
MVP — one Free web service, one Free static site, and one Free PostgreSQL database — plus **one
team-approved cron service** (`ai-daily-digest-intelligence`), originally for the first live
intelligence-pipeline rehearsal and now (issue #113) running the combined daily
ingestion + intelligence pipeline. The cron is the only paid resource in the Blueprint; Resend is
still not part of it. See "Daily pipeline cron (ingestion + intelligence)" below for the approval,
schedule, first-run procedure, and exit-code contract.

## Render foundation

### Service boundaries and configuration

| Process | Build/start boundary | Environment | Owner |
|---|---|---|---|
| FastAPI web service | `pip install uv && uv sync --locked --no-dev --no-editable`; `scripts/start_render.sh` runs `alembic upgrade head` and then replaces itself with the Uvicorn factory | `FRONTEND_ORIGIN` (required public origin), `DOCS_ENABLED` (non-secret), `PAGINATION_CURSOR_SECRET` (secret, generated by Render), `DATABASE_URL` (secret Render database reference) | Person C configures Render; the team never copies secret values into Git or the frontend. |
| React/Vite static site | `npm ci && npm run build`; publish `frontend/dist` | `VITE_API_BASE_URL` (public API origin) | Person C |
| PostgreSQL | Free Render Postgres declared in `render.yaml`; Alembic owns schema changes | `DATABASE_URL` is injected with a `fromDatabase` reference and normalized to the async psycopg scheme at the typed configuration boundary | Person A owns persistence behavior and, from 9 September 2026, the Render resource configuration. |
| Daily pipeline cron (`ai-daily-digest-intelligence`) | `pip install uv && uv sync --locked --no-dev --no-editable`; `.venv/bin/run-daily-digest` (the already-built console script, run directly — not `uv run`, no flags) on the `0.5c-512mb` plan, daily at 06:00 UTC. Collects the three verified RSS sources, then runs intelligence over the real collection start/finish boundary (not a fixed previous-day window), capped at five snapshots per run. Skips the intelligence stage entirely on a healthy zero-new-snapshots day. Reads/writes through the existing ingestion and intelligence persistence layers unchanged (issue #53, extended by issue #113). | `DATABASE_URL` (Render `fromDatabase` reference, never copied), `ANTHROPIC_API_KEY` (`sync: false` — entered only in the Render dashboard, never committed or printed) | Person B owns the LLM adapter behavior; Person A owns the Render cron resource, its environment values, and the combined orchestrator. |
| Resend (later) | HTTPS provider adapter only; not implemented by this foundation | `EMAIL_PROVIDER_API_KEY` (secret), `EMAIL_FROM_ADDRESS` (non-secret sender identity) | Person A owns the remaining Resend provider setup (issue #53, 9 September 2026 handoff). |

`FRONTEND_ORIGIN` is one exact origin such as `https://example.onrender.com`, with no path,
trailing slash, wildcard, credentials, query, or comma-separated alternatives. Non-local HTTP
origins fail configuration validation. CORS allows only this origin, only the currently required
GET/POST methods and headers, and never allows browser credentials. `VITE_API_BASE_URL` is public
build-time configuration and must never contain a secret.

The production factory reads and validates environment variables only when Uvicorn calls it. It
creates one async engine and session factory for the process, mounts `/v1/updates` through a
short-lived session per request, and disposes the engine at shutdown. PostgreSQL is a required
readiness dependency: a failed bounded probe makes readiness return the safe `503` envelope while
liveness continues to describe only the HTTP process.

### Local production-command verification

From a clean checkout on Python 3.12:

```bash
uv sync --locked --no-dev --no-editable
make db-up
make db-migrate
FRONTEND_ORIGIN=http://localhost:3000 \
PAGINATION_CURSOR_SECRET=replace-with-at-least-32-local-bytes \
DATABASE_URL=postgresql+psycopg://ai_daily_digest:ai_daily_digest@localhost:5432/ai_daily_digest \
  .venv/bin/uvicorn ai_daily_digest.delivery.api.production:create_production_app \
  --factory --host 127.0.0.1 --port 8000
```

In a second terminal, verify both probes:

```bash
curl --fail --show-error http://127.0.0.1:8000/v1/health/live
curl --fail --show-error http://127.0.0.1:8000/v1/health/ready
curl --fail --show-error http://127.0.0.1:8000/v1/updates
```

The health bodies are `{"status":"ok"}` and a ready result containing the coarse `database`
check. The updates response contains `items` and `next_cursor`. The committed contract tests also
verify that `render.yaml` points to an importable Uvicorn factory, uses the liveness path, declares
the three Free resources plus exactly the one approved cron service (and no second cron, worker, or
disk), injects both `DATABASE_URL` values by reference, and commits no secret value.

### First deployment

1. Merge only after `make ci` and `cd frontend && npm ci && npm run check` pass on the final branch.
2. In Render, create or sync the Blueprint from the GitHub repository and select `render.yaml`.
   Confirm that the plan shows exactly one Free web service, one Free static site, one Free
   PostgreSQL database, and the one approved cron service `ai-daily-digest-intelligence` on the
   `0.5c-512mb` plan; cancel if it proposes a **second** cron, a worker, a disk, a larger plan,
   or a payment-backed overage.
3. Set `FRONTEND_ORIGIN` to the exact HTTPS static-site origin and `VITE_API_BASE_URL` to the exact
   HTTPS API origin. Render generates `PAGINATION_CURSOR_SECRET`; do not reveal or copy its value.
   Set `ANTHROPIC_API_KEY` only on the cron service (see "Daily pipeline cron (ingestion + intelligence)").
4. If Render assigns a hostname different from the expected service name, correct both public
   origin values and redeploy. Never relax CORS to a wildcard to work around a mismatch.
5. Confirm the start-script migration succeeded before Uvicorn began accepting traffic. Then confirm
   `/v1/health/live`, `/v1/health/ready`, `/v1/updates`, `/openapi.json`, and the frontend root over
   HTTPS. Confirm an allowed-origin preflight succeeds, a lookalike origin is rejected, and no
   response contains `Access-Control-Allow-Credentials: true`.
6. Record the deployed commit SHA and smoke-test outcome in the issue or PR. Do not record
   environment values, headers, subscriber addresses, or credentials.

### Rollback

1. Select the last known-good deploy for the API and static site in Render, using the same recorded
   commit SHA for both when possible. Do not change or reveal secret values during rollback.
2. If `render.yaml` caused the failure, revert that repository commit through a reviewed PR and let
   the Blueprint reconcile. Do not patch `main` directly.
3. Repeat the HTTPS smoke checks above, including exact-origin CORS and readiness.
4. If a database migration is involved, stop the rollout and follow that migration's documented
   downgrade/forward-fix policy. Prefer a reviewed forward fix; never run a destructive downgrade
   against the only copy of MVP data merely to make a web deploy appear healthy.

### Database deployment gate

Render documents its dedicated pre-deploy command as paid-only. On the Free web service,
`scripts/start_render.sh` therefore applies the reviewed Alembic history before starting Uvicorn;
if migration fails, the HTTP process never starts. This is an MVP compromise, not zero-downtime
migration orchestration. The Free database has no managed backups and expires, so it is demo data
only; export or recreate it through the approved pipeline. Before calling this a production
deployment, ADR 0002 still requires separate migration-owner and restricted runtime roles.
Render's generated Free database credential is not evidence that this role split exists. Record
that gate explicitly and do not weaken database privileges or migration history to make a deploy
pass.

### Daily pipeline cron (ingestion + intelligence)

**Team approval.** The team explicitly approved **one** Render cron service,
`ai-daily-digest-intelligence`, and its **minimum USD 1/month** charge, originally so the first
live intelligence-pipeline rehearsal (issue #53) could run on real infrastructure. Issue #113
extended that same one service — no new resource — to also perform the daily RSS collection this
project previously only ran manually. This supersedes the earlier "no cron job / zero cost"
position for this one service only. Render bills cron services **by runtime**, so the actual
monthly charge can exceed the USD 1 minimum: the team must monitor the Render **Runs** page for
this service and must **not** add a second cron service, raise its plan, or change its schedule
without a new recorded team decision. Person A owns this resource and its environment values
(issue #53, 9 September 2026 handoff; issue #113).

**Command.** `startCommand` is `.venv/bin/run-daily-digest` — the console script the non-editable
build already installed into `.venv`, invoked directly with no flags. It is deliberately **not**
`uv run run-daily-digest`: `uv run` can resynchronize the environment at runtime and could pull
development or editable dependencies into every paid cron execution.

**Why not just chain `collect-rss-batch` and `generate-digest`.** The earlier rehearsal cron ran
`generate-digest --previous-complete-utc-day --since 24h --limit 5` alone; it never collected
sources. Simply adding a `collect-rss-batch` step in front of that command would have been
silently wrong: snapshots a run's own collection step creates carry `fetched_at = now` (today, in
whatever partial hour the cron fires), while `--previous-complete-utc-day` selects **yesterday's**
whole UTC day. Those brand-new snapshots would never fall in that window, so freshly collected
evidence would be skipped **every single day**, permanently. `run-daily-digest`
(`src/ai_daily_digest/daily_pipeline.py`) fixes this by using the collection stage's own recorded
`started_at`/`finished_at` (plus a one-second safety margin on the end, to guard a clock-tie edge
case) as the intelligence selection window instead of any date arithmetic — see that module's
docstring for the full explanation.

**Schedule.** `schedule: "0 6 * * *"` — 06:00 UTC daily, unchanged by issue #113. Render always
interprets cron schedules in UTC; there is no timezone setting.

**What one run does, in order:**

1. Collect the verified production RSS sources — `openai_news`, `langchain_pypi`,
   `langgraph_pypi` — through the existing, unmodified `ingestion.rss.batch.run_rss_batch`. Same
   per-source timeout/retry/backoff, host allowlist, URL canonicalization, content hashing,
   snapshot deduplication, and per-source failure isolation as a manual `collect-rss-batch` run.
2. If every selected source failed outright, stop here — no intelligence run, no digest touched.
3. If collection succeeded (or partially succeeded) but created **zero** new snapshots — every
   entry was already seen — stop here too: this is a **healthy `no_updates` day**, not an error,
   and the digest tables are never touched. This is what guarantees an empty digest is never
   published: intelligence is simply never invoked when there is nothing new to process.
4. Otherwise, run the existing, unmodified `intelligence.run.run_pipeline` over the real
   collection window described above, digest date = today's UTC date, capped at **five**
   snapshots (the same cost control the old `--limit 5` rehearsal used) — ordered by
   `fetched_at`, then id, exactly as before.
5. Combine both stages' outcomes into one report (see the exit-code table below) and print a
   single line of JSON.

**Manual runs behave identically.** A manually **Triggered Run** from the Render Runs page
executes the same `startCommand`, so it collects the same three sources and processes whatever
window that particular collection attempt produces.

**Scope.** This cron now performs **both** ingestion and intelligence, but only for the three
verified RSS sources above. Collecting any *other* registered source, or an ad-hoc/backfill run,
still requires an authorised operator's local CLI (`collect-rss`, `collect-rss-batch
--source-id ...`, or `collect-official-article`) — never an unauthenticated public endpoint.

**First-run / redeploy procedure:**

1. Merge this change and sync the Render Blueprint from `render.yaml`. The Blueprint diff should
   show only the existing three services updating their `startCommand`/build, with **no** new
   resource — cancel if it proposes a second cron, a worker, a disk, a larger plan, or any other
   paid resource.
2. Confirm `ANTHROPIC_API_KEY` is already set on the cron service (it was set during the original
   issue #53 first-run procedure); `DATABASE_URL` continues to be injected automatically from the
   database's `connectionString` and must never be opened, copied, or exposed.
3. Trigger a run from the service's **Runs** page with **Trigger Run** to verify the new command
   before waiting for the next scheduled 06:00 UTC firing.
4. Capture only the **final single-line JSON report** printed to stdout and the **process exit
   code**. Record those in the issue/PR. Do **not** share raw logs, prompt text, database
   credentials, connection strings, or API keys — the report line is safe to share; the
   surrounding log output is not.

**Combined exit codes** (`run-daily-digest`):

| Exit code | Combined status | Meaning |
|---|---|---|
| `0` | `published` | New snapshots collected and intelligence published a digest. |
| `0` | `clean_zero_change` | New snapshots collected and fully processed; provably no changes or claims (PR #111 semantics). |
| `0` | `no_updates` | Collection succeeded but created no new snapshots. Intelligence never runs; no digest is touched. |
| `1` | `partial` | Collection partial (at least one source failed) — reported even if the sources that *did* succeed produced a clean or published intelligence result — or intelligence itself returned partial. |
| `1` | `review` | Intelligence routed the digest to review. |
| `2` | `failed` | Collection configuration/total failure (every selected source failed, or the process could not start — e.g. bad `DATABASE_URL`), or a fatal intelligence failure. |

A `partial` collection is **always** reported as `partial`, never silently upgraded to a healthy
exit code by a clean intelligence result on the data that did get collected — the operator must
still see that a source is failing. A `review`/`partial` intelligence outcome is a **valid** run,
not a failure to fix by loosening the pipeline; this module's publication and safety gates are
never weakened to force a publish.

## Render free-tier constraints

Render documents the current free allowances and limitations in
[Deploy for Free](https://render.com/docs/free):

- Static Sites are free, subject to the workspace's included bandwidth and build-pipeline usage.
- A workspace receives 750 free web-service instance hours per calendar month.
- A free web service spins down after 15 minutes without inbound traffic. Its next request can be
  delayed while the service starts again.
- Free web services have an ephemeral filesystem and cannot attach a persistent disk.
- Free web services cannot make outbound SMTP connections on ports 25, 465, or 587. Email delivery
  must therefore use Resend's HTTPS API, not SMTP.
- Only one free Render Postgres database can be active per workspace. It has 1 GB of storage, no
  backups, no managed connection pooling, and expires 30 days after creation. After the documented
  14-day upgrade grace period, Render deletes it.
- Render may restart or maintain free services and databases at any time. The free plans are for
  preview and hobby use, not production availability.

Render [Cron Jobs](https://render.com/docs/cronjobs) are billed by runtime and have a minimum
monthly charge of USD 1 per cron service. The team has **accepted that cost for exactly one cron
service**, `ai-daily-digest-intelligence`, now running the combined daily ingestion +
intelligence pipeline (see "Daily pipeline cron (ingestion + intelligence)" above). Runtime
billing can exceed the USD 1 minimum, so the team
monitors this service's Runs page. Any **additional** cron service, a worker, a plan increase, or
a schedule change still requires a new recorded team decision. All other scheduled or ad-hoc
pipeline work stays on an authorised operator's local CLI.

### Render operating rules for the MVP

1. Select `free` explicitly for the web service and PostgreSQL; do not rely on dashboard defaults.
2. The only approved paid resource is the single `ai-daily-digest-intelligence` cron service on
   the `0.5c-512mb` plan. Do not create a second cron service, a worker, a disk, a larger
   database, or any other paid resource without a recorded team decision.
3. Trigger the daily pipeline through the approved cron service's Runs page when verifying a
   change. Ad-hoc or debugging pipeline runs (including collecting a source outside the verified
   three) may still be run from an authorised team member's local checkout with values supplied
   through that operator's untracked local environment; never expose the command through an
   unauthenticated public endpoint.
4. Warm the API with a health request shortly before a live demonstration, while still treating
   cold-start behavior as expected rather than masking readiness failures.
5. Export through an approved migration process or move to durable PostgreSQL before the 30-day
   expiry. The free database is not the long-term system of record promised by ADR 0002.
6. Keep the application stateless outside PostgreSQL; never rely on Render's local filesystem.

### Manual pipeline execution boundary

The daily ingestion + intelligence pipeline now runs on the approved Render cron service
(`ai-daily-digest-intelligence`, `run-daily-digest`), scheduled and also triggerable from Render's
Runs page. That command only ever touches the three verified RSS sources. Outside that one
service, an authorised team member still runs the individual collection/digest CLI commands
(`collect-rss`, `collect-rss-batch`, `collect-official-article`, `generate-digest`) from a local
repository checkout for ad-hoc work, an unverified source, or debugging, supplying deployment
configuration through an untracked local environment and monitoring the command directly. Neither
path is an API route: no command here may be wrapped in or exposed through an unauthenticated
public HTTP endpoint. Any **public or remote HTTP** trigger — as opposed to Render's own
authenticated scheduler and Runs page — still requires a separate security decision covering
authentication, authorisation, replay protection, rate limits, audit logging, and secret access.

#### Controlled historical-article rehearsal

After the official-article backfill change is merged and the existing
`ai-daily-digest-intelligence` cron service shows that exact `main` commit as
its latest successful build, an authorised operator may create a **one-off
job on that existing service** with this command:

```bash
.venv/bin/collect-official-article --source-id anthropic_news --url https://www.anthropic.com/news/100k-context-windows --url https://www.anthropic.com/news/claude-2-1
```

This does not change the cron's configured start command or schedule and must
not create another Render service. The pages are intentionally supplied oldest
first: Anthropic's 11 May 2023 100K context-window announcement establishes the
baseline, followed by its 21 November 2023 Claude 2.1 200K announcement. The
data remains real first-party evidence; no facts are pasted or synthesised.

Record only the command's final secret-safe JSON line and exit status. A clean
first run should report `status: ok`, `requested_count: 2`,
`processed_count: 2`, and two created snapshots; an idempotent rerun may instead
report them unchanged. If collection succeeds, copy the report's `started_at`
value and create a second one-off job on the same service:

```bash
.venv/bin/generate-digest --since <article-report-started_at> --limit 5 --digest-date <current-UTC-date>
```

Success for the full rehearsal means the intelligence report contains a
grounded change and claim, the publication gates allow a published digest, and
the stored result appears through `/v1/changes`, `/v1/digests`, and the UI. A
draft or review-required outcome is still a valid fail-closed result and must
be investigated from its safe reason codes; never weaken the gates merely to
force a demonstration.

## Resend free-tier constraints

Resend's current [pricing](https://resend.com/pricing) and
[quota documentation](https://resend.com/docs/knowledge-base/account-quotas-and-limits) list:

- 3,000 transactional emails per month;
- 100 transactional emails per day;
- three verified domains on the Free plan;
- 30 days of activity-data retention; and
- no free-plan overage. Sending pauses when the free allowance is exhausted.

Each `To`, `CC`, or `BCC` recipient counts separately. The MVP must therefore test only with the
three approved team recipients or Resend's documented
[test addresses](https://resend.com/docs/dashboard/emails/send-test-emails), and it must not send to
a real subscriber list.

Resend requires an API key for the HTTPS API. Create a sending-only, domain-restricted key where
possible and store it only in the deployment secret environment. Resend displays a newly created
key once, so the key owner must record and rotate it through the agreed secret-management process.
See [Resend API keys](https://resend.com/docs/dashboard/api-keys/introduction).

Sending to recipients beyond the account's restricted test case requires a domain the team owns.
The DNS owner must publish and verify the SPF and DKIM records described in
[Managing Domains](https://resend.com/docs/dashboard/domains/introduction). Domain registration is
separate from Resend and may introduce a cost if the team does not already own one.

### Resend operating rules for the MVP

1. Use the HTTPS email API, never SMTP from the Render service.
2. Use the provider adapter required by the architecture; domain logic must not import a Resend SDK.
3. Keep an application-side daily safety limit below the provider's 100-message cap.
4. Preserve delivery idempotency so retries cannot consume the quota or send duplicate messages.
5. Log provider message identifiers and safe status metadata, never raw tokens or subscriber email
   addresses.
6. Use team/test recipients until the domain, consent, confirmation, unsubscribe, and suppression
   paths have passed their tests.

## Alternatives considered

The existing boundaries keep these providers replaceable. An alternative should still expose
PostgreSQL through `DATABASE_URL`, run the FastAPI ASGI service, use environment-backed secrets,
and sit behind the delivery email adapter.

### Application and database hosting

| Option | Current free entry point | Relevant trade-offs | Fit for this MVP |
|---|---|---|---|
| Render | Free Static Site, Web Service, and temporary PostgreSQL | One dashboard and simple Git deployment, but cold starts and a database that expires after 30 days. Cron costs at least USD 1/month. | **Recommended for the seven-day demo.** |
| Railway | Free plan with USD 1 of monthly usage credit and one small replica | The credit can be exhausted by an API/database combination; stronger workspace collaboration is associated with paid plans. See [Railway plans](https://docs.railway.com/pricing/plans). | Convenient fallback, but less predictable as a zero-cost shared stack. |
| Koyeb | One free 512 MB Web Service and one free database limited to 5 compute hours per month | The free database sleeps after 5 minutes of inactivity and currently supports PostgreSQL 14–16, not the PostgreSQL 17 baseline required by ADR 0002. The free service scales to zero after one hour; one free application instance cannot also provide a separate worker; Starter organizations are limited to one member and require a valid payment method. See [instances](https://www.koyeb.com/docs/reference/instances), [databases](https://www.koyeb.com/docs/databases), and [organizations](https://www.koyeb.com/docs/reference/organizations). | **Not a direct fallback.** Requires PostgreSQL compatibility validation and explicit architecture approval before use, in addition to accepting weaker team ergonomics. |
| Render plus Supabase PostgreSQL | Render hosts the UI/API; Supabase Free supplies a 500 MB PostgreSQL database | Avoids Render's 30-day database expiry, but introduces a second provider. Free Supabase projects pause after one week of inactivity and have no automatic backups. See [Supabase pricing](https://supabase.com/pricing). | Best zero-cost fallback if the database must survive beyond 30 days. |

Do not switch database providers after migrations begin without checking PostgreSQL version,
extension, connection, migration, backup, and data-export compatibility. In particular, Koyeb's
current PostgreSQL 14–16 range does not satisfy ADR 0002's PostgreSQL 17 baseline without an
approved compatibility decision. ADR 0002 remains authoritative for the storage model regardless
of host.

### Transactional email

| Option | Current free entry point | Relevant trade-offs | Fit for this MVP |
|---|---|---|---|
| Resend | 3,000 emails/month and 100/day | Clear HTTPS API, sufficient MVP quota, domain verification required for general recipients. | **Recommended.** |
| MailerSend | 500 emails/month and 100 API requests/day | One domain, one user seat plus accountant role, one-day activity retention, and the Free plan requires account approval and payment-card details. See [MailerSend pricing](https://www.mailersend.com/pricing). | Capable fallback, but lower quota and more account friction. |
| Postmark | Non-expiring Developer plan with 100 emails/month | Excellent for a small technical demonstration but too restrictive for even modest subscriber growth. See [Postmark pricing](https://postmarkapp.com/pricing/). | Testing fallback only. |
| Amazon SES | Usage-priced service; new AWS accounts can apply general introductory credits | More infrastructure, identity, billing, and operational setup. The former SES-specific free tier is no longer available to new customers. See [Amazon SES pricing](https://aws.amazon.com/ses/pricing/). | Not selected for the seven-day MVP. |

Changing email providers must not weaken ADR 0012. Confirmation and unsubscribe tokens, consent
generation, privacy-preserving responses, idempotency, and secret-handling requirements remain the
same.

## Upgrade triggers

The team must revisit the provider decision before any of these events:

- the API must remain responsive without free-tier cold starts;
- PostgreSQL data must remain available beyond the 30-day MVP window or require backups;
- automated scheduling beyond the single approved intelligence-rehearsal cron is required (for
  example a second cron for ingestion, or ingestion-plus-intelligence orchestration);
- expected transactional email exceeds 100 recipients per day or 3,000 per month;
- a real subscriber list replaces controlled team/test recipients;
- availability, support, audit, data-retention, or recovery requirements become production-grade;
- a provider asks for a payment method or enables usage-based overages.

At that point, record the expected monthly cost, owner, spending cap, rollback/export path, and
non-author approval before enabling the paid resource.

## Access and ownership status

Never place an account identifier, key, token, URL containing credentials, or personal data in
this table.

| Access or decision | Owner | Status |
|---|---|---|
| Render account ownership | Person C | Account created and ownership confirmed. |
| Remaining Render and Resend provider setup and production environment values | Person A | Handed off from Person C on 9 September 2026 (issue #53). Person C's merged delivery foundations remain in use. |
| Render Blueprint (API, static site, PostgreSQL) | Person A configures; Person C account owner | Blueprint deployed and connected to the repository. The API health endpoint and the PostgreSQL database are healthy. |
| Render intelligence cron (`ai-daily-digest-intelligence`) | Person A | Team-approved (one cron, min USD 1/month). Declared in `render.yaml`; the resource is created on the next Blueprint sync and its first run is triggered manually. |
| Resend account and future sending-only API-key rotation | Person A | Account exists; no production key or send is authorised by this foundation. |
| Resend sending domain and DNS changes | To be named by the team | **Pending before non-test recipients can be used.** |
| Allowed frontend origin and public frontend/API URL pairing | Person A | Configuration names and validation are defined; deployed origin values are set in Render only. |
| Database credential | Person A for persistence behavior and Render injection | `DATABASE_URL` is injected by Render `fromDatabase`; it is never copied out of Render. |
| LLM provider credential | Person B for adapter behavior; Person A for the Render cron injection | `ANTHROPIC_API_KEY` (`sync: false`) is entered only in the `ai-daily-digest-intelligence` cron's Render dashboard; not yet set. |
| Cost policy | Person A configures; non-author review for changes | One approved paid resource: the `ai-daily-digest-intelligence` cron (runtime-billed, min USD 1/month). No second cron, worker, disk, larger database, automatic overage, or production campaign without a new recorded decision. |

The remaining pending row (Resend sending domain / DNS ownership) is an operational gate, not a
reason to commit guessed values: no general-recipient email send may proceed past it. The
intelligence cron follows the first-run procedure above: the Blueprint sync **creates** the cron
service, Person A then **immediately sets `ANTHROPIC_API_KEY`** in that service's Render
environment, and only after the key is configured is the first execution **manually triggered**.
`DATABASE_URL` needs no manual step — Render injects it automatically from the database. The first
run's result is recorded on issue #53.

Subscription activation has two additional fail-closed gates under ADRs 0012 and 0013. Person A
must wire a provider-neutral confirmation-delivery adapter so the raw confirmation capability is
passed directly to the provider after the database transaction and is never returned, persisted,
or logged. Person A must also configure Uvicorn/Render with an explicit trusted-proxy allowlist and
verify that `request.client` contains the validated public client address; wildcard proxy trust is
not allowed. Until both conditions are reviewed and tested under issue #53, the production factory
keeps all subscription routes unmounted even when subscription security keys are present.
The static frontend likewise keeps `VITE_SUBSCRIPTIONS_ENABLED` false or unset until those gates
are complete, so production does not display controls backed by an unavailable endpoint.
